public class Main {
	Deck deck = new Deck();
	Hand[] hands = new Hand[2];
	hands[0] = new Hand(); hands[1] = new Hand();
	public void deal() {
		while (deck.size() > 0) {
			hands[0].put(deck.getCard());
			hands[1].put(deck.getCard());
		}
	}
	
	public void playRound() {
		
	}
	
	public void playWar() {
		deal();
		while (!hands[0].isEmpty() && !hands[1].isEmpty()) {
			
		}
	}
}
